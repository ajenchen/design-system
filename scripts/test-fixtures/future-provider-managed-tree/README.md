# Future provider managed tree fixture

This nonempty tree proves that a registered provider-owned product tree is copied byte-for-byte
from one canonical source into the signed npm corpus, a new scaffold, and an existing consumer.
