---
description: Retired command compatibility pointer
---

# `/gov-status` retired

This one-time Genesis compatibility file has no executable workflow. Use the
provider-neutral `governance-status` skill. The distinct protected cleanup PR
removes this legacy command path after the A/M-only transition closes.
