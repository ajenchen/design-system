#!/bin/bash
# One-time control-plane Genesis compatibility tombstone.
#
# Cross-repository propagation is owned by the provider-neutral release and
# adoption lifecycle. This legacy provider hook intentionally performs no
# operation and is removed by the distinct protected cleanup PR after the
# A/M-only Genesis transition closes.
exit 0
