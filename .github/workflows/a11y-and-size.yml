name: A11y + Bundle Size

on:
  workflow_dispatch:
  schedule:
    # Non-blocking drift monitor; merge safety is owned by the single CI gate.
    - cron: '0 2 * * *'

permissions:
  contents: read

defaults:
  run:
    shell: bash -eo pipefail {0}

jobs:
  a11y-axe:
    name: Monitor a11y drift(axe-core WCAG 2A+AA)
    runs-on: ubuntu-24.04
    env:
      # setup:playwright 以 PLAYWRIGHT_BROWSERS_PATH=0 安裝到 repo-local
      # node_modules/playwright-core/.local-browsers;消費步驟必須帶同一 pin,
      # 否則 playwright 落回 ~/.cache/ms-playwright 找不到瀏覽器(2026-07-28 CI 錨例)。
      PLAYWRIGHT_BROWSERS_PATH: '0'
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: '24.14.0', cache: 'npm' }
      - run: npm run --silent setup:dependencies
      - run: npm run --silent setup:playwright -- --with-deps
      - name: Build DS lib + types(storybook-config tsc + Storybook 前置)
        run: npm run build:lib 2>&1 | tail -10
      - run: npm run build-storybook 2>&1 | tail -20
      - name: Report axe-core baseline drift
        run: npm run a11y:check -- --gate 2>&1 | tail -60
      - name: Upload a11y report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: a11y-audit-report
          path: .git/governance-runtime/evidence/audit/a11y-audit.json
          include-hidden-files: true
          if-no-files-found: warn

  size-limit:
    name: Monitor bundle size drift
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: '24.14.0', cache: 'npm' }
      - run: npm run --silent setup:dependencies
      - name: Build DS lib + types(含 storybook-config,supply .storybook/main.ts 型別)
        run: npm run build:lib 2>&1 | tail -10
      - name: Build production app(for aggregate budget)
        run: npm run build 2>&1 | tail -10
      - name: Run size-limit
        run: npx --no-install size-limit 2>&1 | tail -60
